# TWA / Android build artifacts
twa/keystore/
twa/*.jks
twa/android/
twa/output/
twa/*.aab
twa/*.apk
# Gradle build output + cache — regenerated by `bubblewrap build`, never commit
twa/.gradle/
twa/build/
twa/app/build/
twa/*.idsig

# Environment (local secrets — never commit)
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local
frontend/.env.*.local

# DB reset script — destructive, never commit to shared branches
backend/src/config/reset.sql

# Personal scratchpad — may contain secrets, never commit
to-do.md

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.iml

# Local credential notes - never commit (Tobi, 2026-08-04)
cloudflare-auth.md
